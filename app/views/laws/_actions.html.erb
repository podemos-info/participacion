<% if can? :hide, law %>
<%= link_to t("admin.actions.hide").capitalize, hide_moderation_law_path(law),
              method: :put, remote: true, data: { confirm: t('admin.actions.confirm') } %>
<% end %>

<% if can? :hide, law.author %>
  &nbsp;|&nbsp;
<%= link_to t("admin.actions.hide_author").capitalize, hide_moderation_user_path(law.author_id),
              method: :put, data: { confirm: t('admin.actions.confirm') } %>
<% end %>
