<%= render 'proposals/votes',
           { proposal: proposal, vote_url: vote_proposal_path(proposal, value: 'yes') } %>

<%= render 'enquiries/votes',
{ enquiry: enquiry, vote_url: vote_enquiry_path(enquiry, value: 'yes') } %>
